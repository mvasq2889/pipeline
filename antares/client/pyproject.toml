# Guide (user-friendly):
# https://packaging.python.org/en/latest/guides/writing-pyproject-toml/

# Specification (technical, formal):
# https://packaging.python.org/en/latest/specifications/pyproject-toml/


# Choosing a build backend:
# https://packaging.python.org/en/latest/tutorials/packaging-projects/#choosing-a-build-backend
[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"

# Backend specific configuration
[tool.pdm.build]
includes = [
    "LICENSE",
    "antares_client/",
]

[project]
name = "antares-client"

# Versions should comply with PEP 440:
# https://www.python.org/dev/peps/pep-0440/
#
# For a discussion on single-sourcing the version, see
# https://packaging.python.org/guides/single-sourcing-package-version/
version = "1.14.0"

description = "A light-weight client for receiving alerts from ANTARES."

readme = "README.md"

# Specify which Python versions you support. In contrast to the
# 'Programming Language' classifiers in this file, 'pip install' will check this
# and refuse to install the project if the version does not match. See
# https://packaging.python.org/guides/distributing-packages-using-setuptools/#python-requires
requires-python = ">=3.9"

license = { file = "LICENSE" }

keywords = ["antares"]

authors = [{ name = "NSF NOIRLab ANTARES Team", email = "antares@noirlab.edu" }]

maintainers = [
  { name = "NSF NOIRLab ANTARES Team", email = "antares@noirlab.edu" },
]

# Classifiers help users find your project by categorizing it.
#
# For a list of valid classifiers, see https://pypi.org/classifiers/
classifiers = [
  "License :: OSI Approved :: BSD License",
  "Programming Language :: Python :: 3.9",
]

# This field lists other packages that your project depends on to run.
# Any package you put here will be installed by pip when your project is
# installed, so they must be valid existing projects.
#
# For an analysis of this field vs pip's requirements files see:
# https://packaging.python.org/discussions/install-requires-vs-requirements/
dependencies = [
    "astropy",
    "bson",
    "click",
    "marshmallow>=3.6,<4",
    "marshmallow-jsonapi>=0.23",
    "numpy",
    "pandas",
    "requests",
    "typing-extensions",
    "astropy-healpix",
]

# List additional groups of dependencies here (e.g. development
# dependencies). Users will be able to install these using the "extras"
# syntax, for example:
#
#   $ pip install antares-client[subscriptions]

# Optional dependencies the project provides. These are commonly
# referred to as "extras". For a more extensive definition see:
# https://packaging.python.org/en/latest/specifications/dependency-specifiers/#extras
[project.optional-dependencies]
dev = [
    "black",
    "pylint",
    "isort",
]
test = [
    "coverage==7.2.7",
    "pytest",
    "pytest-cov",
    "requests-mock",
]
subscriptions = [
    "confluent_kafka>=1.7.0,<=2.13.0"
]
docs = [
    "sphinx",
    "sphinx-click",
]

# List URLs that are relevant to your project
[project.urls]
"Homepage" = "https://gitlab.com/nsf-noirlab/csdc/antares/client"
"Documentation" = "https://nsf-noirlab.gitlab.io/csdc/antares/client"
"Bug Reports" = "https://gitlab.com/nsf-noirlab/csdc/antares/client/-/issues"
"Source" = "https://gitlab.com/nsf-noirlab/csdc/antares/client"

# The following would provide a command line executable called `sample`
# which executes the function `main` from this package when invoked.
[project.scripts]
antares = "antares_client.cli:entry_point"

# Pytest
[tool.pytest.ini_options]
addopts = "--cov=antares_client --cov-report term --cov-report xml --junit-xml=report.xml"

# Pylint
[tool.pylint]
disable = ["R", "C", "logging-format-interpolation"]
ignored-classes = ["astropy.units"]

# Isort
[tool.isort]
profile = "black"
